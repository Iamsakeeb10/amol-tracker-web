# CHUNK 8 Implementation Plan — Privacy & Terms Pages + Final Polish

## Overview

CHUNK 8 adds standalone privacy/terms pages, SEO files (sitemap, robots), and applies polish across all sections (scroll animations, Play Store URL updates, mobile responsiveness).

---

## Current State (What Exists)

| File | Status |
|------|--------|
| `src/components/PrivacySection.tsx` | ✅ Exists — has full Bengali/English privacy policy content |
| `src/components/Footer.tsx` | ✅ Exists — links to `/privacy` and `/terms` (currently 404) |
| `src/app/privacy/page.tsx` | ❌ Missing |
| `src/app/terms/page.tsx` | ❌ Missing |
| `src/components/PrivacyPage.tsx` | ❌ Missing |
| `src/components/TermsPage.tsx` | ❌ Missing |
| `src/app/sitemap.ts` | ❌ Missing |
| `src/app/robots.ts` | ❌ Missing |
| Terms & Conditions content | ❌ Missing — no content exists anywhere |

---

## Implementation Steps

### Step 1: Create `src/components/PrivacyPage.tsx`

**Purpose:** Standalone full-page privacy policy component.

**Approach:**
- Extract the privacy content from `PrivacySection.tsx` (both `PrivacyContentBn` and `PrivacyContentEn`)
- Create a new component with:
  - Back button: `← amoltracker.app` linking to `/`
  - Full privacy policy content (both languages)
  - Language toggle support via `useLang()`
  - Same styling as PrivacySection but full-page layout
  - Max-width container, centered content
  - Responsive design with embedded `<style>` tag

**Note:** This component does NOT need to wrap itself with LanguageProvider since the parent route page will handle that.

---

### Step 2: Create `src/app/privacy/page.tsx`

**Purpose:** Route page for `/privacy`.

**Approach:**
- Import `PrivacyPage` component
- Import `LanguageProvider` from `@/context/LanguageContext`
- Wrap `PrivacyPage` with `LanguageProvider` (standalone page needs its own provider)
- Export metadata: `{ title: 'Privacy Policy — Amol Tracker' }`

**Structure:**
```tsx
import { Metadata } from 'next';
import { LanguageProvider } from '@/context/LanguageContext';
import PrivacyPage from '@/components/PrivacyPage';

export const metadata: Metadata = {
  title: 'Privacy Policy — Amol Tracker',
};

export default function Privacy() {
  return (
    <LanguageProvider>
      <PrivacyPage />
    </LanguageProvider>
  );
}
```

---

### Step 3: Create `src/components/TermsPage.tsx`

**Purpose:** Standalone full-page terms & conditions component.

**Approach:**
- Write Bengali and English T&C content (no existing content — need to create)
- Structure similar to PrivacyPage:
  - Back button: `← amoltracker.app` linking to `/`
  - Terms content sections
  - Language toggle support
  - Responsive design

**Terms & Conditions Content Sections:**
1. শর্তাবলী স্বীকার / Terms Acceptance
2. সেবা বর্ণনা / Service Description
3. ব্যবহারকারী দায়িত্ব / User Responsibilities
4. বুদ্ধিবৃত্তিক সম্পত্তি / Intellectual Property
5. সীমাবদ্ধ দায় / Limitation of Liability
6. সেবা পরিবর্তন / Changes to Service
7. যোগাযোগ / Contact

---

### Step 4: Create `src/app/terms/page.tsx`

**Purpose:** Route page for `/terms`.

**Approach:**
- Import `TermsPage` component
- Import `LanguageProvider`
- Wrap `TermsPage` with `LanguageProvider`
- Export metadata: `{ title: 'Terms & Conditions — Amol Tracker' }`

---

### Step 5: Create `src/app/sitemap.ts`

**Purpose:** Generate sitemap.xml for SEO.

**Approach:**
- Import `MetadataRoute` from `next`
- Export default function returning array of URLs:
  - `https://amoltracker.app` (priority 1, monthly)
  - `https://amoltracker.app/privacy` (priority 0.5, yearly)
  - `https://amoltracker.app/terms` (priority 0.5, yearly)

---

### Step 6: Create `src/app/robots.ts`

**Purpose:** Generate robots.txt for search engines.

**Approach:**
- Import `MetadataRoute` from `next`
- Export default function returning rules object:
  - User agent: `*`
  - Allow: `/`
  - Sitemap: `https://amoltracker.app/sitemap.xml`

---

### Step 7: Add IntersectionObserver Animations to All Sections

**Purpose:** Add smooth fade-in/slide-up animations when sections enter viewport.

**Approach:**
Add to each section component (Stats, Features, Screenshots, About, Download, PrivacySection, Footer):

```tsx
'use client';
import { useEffect, useRef, useState } from 'react';

// Inside component:
const ref = useRef<HTMLElement>(null);
const [visible, setVisible] = useState(false);

useEffect(() => {
  const obs = new IntersectionObserver(
    ([e]) => { if (e.isIntersecting) setVisible(true); },
    { threshold: 0.1 }
  );
  if (ref.current) obs.observe(ref.current);
  return () => obs.disconnect();
}, []);

// Apply to <section> element:
<section
  ref={ref}
  style={{
    opacity: visible ? 1 : 0,
    transform: visible ? 'translateY(0)' : 'translateY(24px)',
    transition: 'opacity 0.6s ease, transform 0.6s ease',
    // ... existing styles
  }}
>
```

**Files to update:**
- `src/components/Stats.tsx`
- `src/components/Features.tsx`
- `src/components/Screenshots.tsx`
- `src/components/About.tsx`
- `src/components/Download.tsx`
- `src/components/PrivacySection.tsx`
- `src/components/Footer.tsx`

**Note:** Navbar and Hero already have their own animations; don't add this pattern to them.

---

### Step 8: Update Play Store URLs

**Purpose:** Replace all `#download` links with actual Play Store URL.

**Search and replace across codebase:**
- Find: `href="#download"` or `href={'#download'}`
- Replace with: `href="https://play.google.com/store/apps/details?id=com.shakib.amol.amol_tracker_app"`

**Files to check:**
- `src/components/Hero.tsx`
- `src/components/Download.tsx`
- `src/components/Navbar.tsx`

---

### Step 9: Verification

**Manual checks:**
1. Run `npm run dev` — no compilation errors
2. Visit `/privacy` — page renders with Bengali/English toggle
3. Visit `/terms` — page renders with Bengali/English toggle
4. Click Footer links — navigate correctly
5. Scroll down on homepage — sections animate in
6. Test mobile (375px) — no overflow, readable text, touch targets ≥44px
7. Language toggle works on all pages
8. `npm run build` — zero errors

---

## Files to Create (6 files)

1. `src/components/PrivacyPage.tsx`
2. `src/components/TermsPage.tsx`
3. `src/app/privacy/page.tsx`
4. `src/app/terms/page.tsx`
5. `src/app/sitemap.ts`
6. `src/app/robots.ts`

## Files to Modify (7+ files)

1. `src/components/Stats.tsx` — add IntersectionObserver animation
2. `src/components/Features.tsx` — add IntersectionObserver animation
3. `src/components/Screenshots.tsx` — add IntersectionObserver animation
4. `src/components/About.tsx` — add IntersectionObserver animation
5. `src/components/Download.tsx` — add IntersectionObserver animation + update Play Store URL
6. `src/components/PrivacySection.tsx` — add IntersectionObserver animation
7. `src/components/Footer.tsx` — add IntersectionObserver animation
8. `src/components/Hero.tsx` — update Play Store URL
9. `src/components/Navbar.tsx` — update Play Store URL (if applicable)

---

## Estimated Effort

- Creating new pages/components: ~40% of work
- Adding animations to 7 sections: ~35% of work
- URL updates: ~10% of work
- Testing/verification: ~15% of work

---

## Risks & Mitigations

| Risk | Mitigation |
|------|-----------|
| Terms content doesn't exist | Write comprehensive bilingual T&C covering essential legal areas |
| Animation performance on mobile | Use `will-change: opacity, transform` and keep threshold at 0.1 |
| LanguageProvider duplication | Privacy/Terms pages each get their own provider (clean separation) |

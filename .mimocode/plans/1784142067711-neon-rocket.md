# Chunk 7 — PrivacySection + Footer Implementation Plan

## Summary

Create `PrivacySection.tsx` and replace the placeholder `Footer.tsx` with the full implementation, per the spec in `PROJECT_IMPLEMENTATION_FILE.md` lines 815–891.

## Files to Modify/Create

| File | Action |
|------|--------|
| `src/components/PrivacySection.tsx` | **CREATE** — new component |
| `src/components/Footer.tsx` | **REPLACE** — currently a 3-line placeholder |
| `src/app/page.tsx` | **EDIT** — add `PrivacySection` import and render between `Download` and `Footer` |

## Component 1: PrivacySection.tsx

**Spec:** `PROJECT_IMPLEMENTATION_FILE.md:827–859`

### Structure
- `<section id="privacy">` with dark background (`rgba(0,0,0,0.15)`), top border, 64px vertical padding
- Max-width 720px centered container with 24px horizontal padding
- Bengali/English toggle via `useLang()` from `LanguageContext`

### Content (hardcoded, not from content.ts)
The spec says to use text from a `privacy_policy.html` file that doesn't exist in the repo. We'll write reasonable privacy policy content inline (both Bengali and English), covering:
- "Last updated" pill badge (gold border)
- Heading: "প্রাইভেসি পলিসি" / "Privacy Policy"
- Sub-sections (h3 with gold color): Data Collection, Data Usage, Data Sharing, Data Security, Children's Privacy, Changes to Policy, Contact
- Paragraphs and bullet lists
- "Full policy" link at bottom pointing to `/privacy` (Chunk 8 will create that page)

### Styling (per spec)
- Heading: 28px, font-weight 700, color white, mb 24px
- Sub-headings (h3): 16px, font-weight 600, color `var(--gold-light)`, mt 24px, mb 8px
- Paragraphs: 14px, color `rgba(255,255,255,0.65)`, line-height 1.8
- Bullet lists: same color, padding-left 20px
- "Last updated" badge: small pill, gold border

### Responsive
- Padding reduces on mobile (via `@media (max-width: 768px)`)

## Component 2: Footer.tsx

**Spec:** `PROJECT_IMPLEMENTATION_FILE.md:863–891`

### Structure
- Background: `rgba(0,0,0,0.3)`, border-top `1px solid rgba(201,168,76,0.15)`, padding 40px 0
- 3 rows centered

### Row 1: App name + tagline
- 🌙 + brand name from `content.nav.brand` in gold color
- `content.footer.tagline` in small muted text

### Row 2: Footer links (horizontal, centered, gap 24px)
- Privacy Policy → `/privacy`
- Terms & Conditions → `/terms`
- Google Play → `#` (placeholder for Play Store URL)
- Each: 13px, color `rgba(255,255,255,0.45)`, hover: `var(--gold-light)`, transition 0.2s

### Row 3: Copyright
- `content.footer.copyright`
- 13px, color `rgba(255,255,255,0.25)`

### Scroll-to-top button (in Footer.tsx)
- Fixed position, bottom-right
- 40px circle, background `var(--gold)`, color `var(--emerald-deep)`
- Shows only when scrolled > 400px
- Smooth scroll to top on click
- `ChevronUp` icon from `lucide-react`

## page.tsx Update

Add `PrivacySection` between `Download` and `Footer`:
```tsx
import PrivacySection from '@/components/PrivacySection';
// ...
<Download />
<PrivacySection />
<Footer />
```

## Patterns to Follow

- Use `'use client'` directive
- Import `content` from `@/lib/content` and `useLang` from `@/context/LanguageContext`
- Use inline `style` props (matching existing component patterns like About.tsx, Download.tsx)
- Use `<style>` tag for responsive media queries and class-based responsive overrides
- Use `className="section-container"` for max-width container

## Verification

1. Run `npm run dev` and visit `http://localhost:3000`
2. Scroll to the bottom — PrivacySection should appear between Download and Footer
3. Footer should show app name, links, and copyright
4. Test language toggle (bn ↔ en) — both components should switch
5. Scroll down past 400px — scroll-to-top button should appear
6. Click scroll-to-top — page should smooth-scroll to top
7. Test responsive at 768px breakpoint — layout should adapt
